#include "monty.h"

/**
 * f_nop - Doesn't do anything
 * @stack: Pointer to the top of te stack
 * @line_number: Line number
 */
void f_nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
